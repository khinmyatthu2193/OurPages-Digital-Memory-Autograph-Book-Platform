import PagePlaceholder from '../components/PagePlaceholder.jsx';

export default function LoginPage() {
  return (
    <PagePlaceholder eyebrow="Owner access" title="Log in">
      Authentication UI will be added with the secure authentication milestone.
    </PagePlaceholder>
  );
}
